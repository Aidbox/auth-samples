### Basic Auth

Node application which access aidbox using Client id and secret.

#### Usage

To do GET request add your box name in 'host' section, path in 'path' section and base64 of your id:secret in 'Authorization' section.
